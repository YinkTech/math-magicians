import React from 'react';

class Button extends React.Component {
    constructor(props) {
        super(props);
        this.state= {name};
    };

    render() {
       { this.state = name.toSting()}
    };
}

export default Button;