import React from "react";
import Operate from "./Operate";

export default function Calculate(obj, buttonName) {
    if (buttonName == obj ("+/-")) {  
        return {
            total: (-1 * total),
            next: (-1 * next),
            operation: substructing,
        };
    };
};
