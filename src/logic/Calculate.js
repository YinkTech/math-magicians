import React from "react";
import Operate from "./Operate";

export default function Calculate(buttonName) {
    if (buttonName == "+/-") {
        return {
            total: (-1 * 'result'),
            next: (-1 * next),
            operations: substructing,
        };
    };
};
